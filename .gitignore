node_modules/
dist/
release/
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.env
.env.*
!.env.example
.DS_Store
.runtime/
.data/
artifacts/
test_audio/
playwright-report/
test-results/
*.log
:memory:.ses

# AI-assistant scaffolding. These are per-developer tooling, not product source:
# skill packs, agent definitions and harness caches differ per machine and per
# assistant, and a checked-in copy silently overrides another developer's setup.
.agents/
.claude/
.codex/
.cursor/
.cursorrules
.aider*
.opencode/
.windsurf/
.continue/
.hermes/
.impeccable/
.skills/
skills-lock.json
CLAUDE.md
GEMINI.md
.github/copilot-instructions.md

# Packaging build artifacts. The icon is regenerated by `npm run icon` and the
# PyInstaller work directory by `npm run build:backend`; neither is source.
backend/build/
build/icon.icns
build/icon.png
