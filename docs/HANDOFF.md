# HANDOFF — SKAZ (AudioHelper)

## ASTRA: восстановление утраченного редизайна из истории Hermes
- Причина установлена по фактическим вызовам: сессия `20260915_233604_25976c`, message12752 → tool result12753 (exit0) выполнила `git checkout --` для AssistantPanel.tsx, SessionList.tsx, Icon.tsx, app.css, tokens.css. Это ошибочный широкий откат после просьбы отменить текущие изменения; объяснение через неизвестного sibling не подтверждено. Message12781 также откатил AssistantPanel.test.tsx.
- Исходники восстановлены из read_file snapshots и успешных exact-match patches текущего профиля state.db (read-only). `.runtime/recovery/manifest.json` содержит SHA256; scripts extract.py/reconstruct.py и restoration.diff сохраняют происхождение. app.css: snapshot12345+12362, затем10успешных CSS/token patches; exact49793→51177bytes и финальные прочитанные диапазоны совпали. tokens snapshot12521+patch12677; AssistantPanel snapshot12637; SessionList snapshot12610; Icon snapshot12620. App snapshot10475+12500 с совпадающим перекрытием/5885bytes уже идентичен рабочему App.tsx — его не меняли.
- Вернулись white/graphite shell, rail/center/assistant, scope-popover, SVG icons, full-bleed artwork/tagline и согласованные panel heights. Незавершённый SessionList message12733 (новые группы/drag/menu без CSS) сохранён отдельно, НЕ применён. Последующая картинка chat-elements-girl.png уже была в рабочем дереве; не синтезировалась заново.
- Проверки: первый frontend run391passed/3failed выявил откат тестов к старому UI; три существующих assertions обновлены (иллюстрация/tagline вместо старого prompt, открыть Auto popover перед scope/window). Финально `npm run test` →394passed/44files; `npm run typecheck` и `npm run build` exit0. `npx playwright test scripts/visual-capture.smoke.spec.ts` →1passed; это capture-only, не assertion-heavy smoke. Просмотрен `.runtime/shots/02-shell.png`: белый трёхколоночный UI восстановлен. Backend/API/реальная речь не проверялись.
- Исторические403frontend tests не восстановлены полностью: AssistantPanel имел14tests до отката, сейчас5. Три существующих теста адаптированы, не выдавать394за403. Не делать бездумный checkout чистого HEAD: восстановленный UI пока uncommitted. Сохранены before/recovered копии, manifest и отдельный recovery ZIP; commit/push не выполнялись. Дальше пользовательская визуальная сверка и отдельное разрешение на checkpoint commit.

## ASTRA: красные детали заменены на #20202A — проверенный UI-срез
- `frontend/src/styles/tokens.css`: прежние бордовый `--accent`, алый `--live` и бордовый `--group-1` сведены к базе `#20202A`; quiet/wash используют тот же RGB с прозрачностью. В тёмной теме применён контрастный оттенок базы `#A4A4B2` (`hover #B8B8C4`), потому что точный `#20202A` на `--paper` давал лишь ~1.11:1 и делал ссылки/индикаторы почти невидимыми. Оба dark-блока синхронны.
- `frontend/src/assets/images/chat-empty-figure.png`: красная ткань и красные элементы иллюстрации перекрашены в графитово-синюю гамму вокруг `#20202A` с сохранением светотени, фактуры, лица, цветов и альфа-канала. PNG остаётся RGBA 800×1200; SHA-256 `406797b74037553b1fb5b687682b481dea2eb381a1938c5591d34f7198846d77`.
- `docs/DESIGN_REDESIGN.md` синхронизирован с новой палитрой и различением live-состояния через форму/подпись/анимацию, а не красный цвет. Impeccable detector по изменённым целям → `[]`.
- Проверка: `npm run test && npm run typecheck && npm run build && npx playwright test scripts/visual-capture.smoke.spec.ts` → **403 passed / 44 files**, typecheck/build passed, visual smoke **1 passed**. Существующие React `act(...)` warnings в ProfileEditor/AssistantPanel остались, новых failures нет. Light screenshots shell/search/settings и отдельный dark screenshot `.runtime/shots/08-dark.png` просмотрены: красных/бордовых акцентов нет, изображение и вёрстка без заметных дефектов, dark controls читаемы. Реальные API/микрофон не запускались; commit/push не выполнялись.

## ASTRA: UI перевода и удаление устаревшего /languages — проверенный срез
- Пользователь разрешил удалить проверки неактуального языкового контракта и подключить перевод вместо оригинала. Удалён только `backend/tests/test_languages_api.py` (35 устаревших cases: model-specific Whisper/OpenAI/unknown catalogs, auto/output picker и validation отсутствующего endpoint). Soniox `test_used_languages`, native configuration/provider tests сохранены; skip/ignore не добавлялись. Текущий UI получает supported_languages из settings, output_language остаётся отдельным полем.
- `frontend/src/api/nativeLive.ts`: типы live translation projection/untimed tokens. `NativeMonologues.tsx::translatedTurns/NativeMonologues`: backend display_token_ids → основной текст, none passthrough в правильном порядке, originals в закрытом details; ожидающий перевод НЕ подменяется оригиналом. Source citation раскрывает оригинал, не создаёт fake timestamps/IDs. Historical/ambiguous translations видны отдельно без guessed turn links; archive originals доступны под раскрытием. `TranscriptView.tsx` использует чистый режим также для исторического translation snapshot без final_tokens. CSS оригинала соответствует существующей ленте.
- `SettingsPanel.tsx`: сохраняемые native mode и target в System; только новые записи, controls disabled при capture/pause/processing; consent не меняется. Три backend-supported режима, для audio-only явно указано отсутствие postprocessing UI. Последний target сохраняется при выключении перевода. Старые experimental controls не рефакторились.
- RED→GREEN UI IPC seam: translation missing, historical fallback missing, settings mode missing. `npm test` →403passed/44files; `npm run typecheck` и `npm run build` passed. `npx playwright test scripts/native-live.smoke.spec.ts scripts/save-on-quit.smoke.spec.ts` →4passed: выбор translation/de через UI→real settings→immutable recording snapshot/Pause/Resume. Отдельный `scripts/translation-ui.smoke.spec.ts` →1passed: built Electron renderer с явно fixture IPC, оригинал скрыт/раскрыт/reload; НЕ real ASR. Screenshots translation-primary.png/translation-original.png в .runtime/soniox-migration.
- Полный `backend/.venv/bin/python -m pytest backend/tests -q --tb=short` →690passed/2dependency warnings, exit0. Логи translation-ui-{backend,frontend}.log. Backend runtime не менялся. Полные Ruff и mypy тоже исполнены: Ruff2ошибки в существующих db.py:153 и routes/sessions.py imports; mypy5ошибок в live_fragments.py:192 и test_local_models_stage2_api.py:26/784/812. Эти файлы в этом срезе не менялись, не исправлялись вне scope; Python quality gates целиком НЕ зелёные.
- Self-review standards/spec самостоятельно, без независимого помощника: immutable recording mode (не текущие settings) управляет отображением; нет word alignment/translation source IDs; unassigned не теряется, старые source IDs сохранены. Graph2026-09-08 stale/not_tracked → direct source reads, без reindex. Kanban env/card нет; работаем root/main в существующем dirty tree. Без реальных API/микрофона/помощников/commit/push.
- Остаётся общей миграции: translation completeness/recovery, bounded snapshots, source revisions/editing, audio-only postprocess, legacy cleanup и реальная ASR/перевод приёмка. Запрос пользователя по translation-primary UI реализован; не выдавать локальные fixtures за качество Soniox.

## ASTRA: backend collection и activity log — проверенный срез
- Пользователь попросил продолжить и устранить отсутствующий `gateways.asr.OPERATION`. RED воспроизведён `backend/.venv/bin/python -m pytest backend/tests/test_activity_log_api.py -q --tb=short` → collection ImportError. Прямые исходники показали отсутствие не только константы, но `/logs`, store и config; требование журнала есть в SESSION-STATE-SPEC§8. Проверки не удалены/не skipped: imports заменены публичными label literals.
- Новые `activity.py::ActivityLog/current_activity/record_attempt`, `gateways/asr_activity.py::observed_transcription/observe_response`, `routes/logs.py::read_logs`; AppConfig capacity256, Runtime-owned ring, app HTTP context binding/reset + authenticated route, chat `_log`→sink, ASR concrete transcribe methods→decorator/response metadata. ContextVar и per-instance lock изолируют параллельные запросы/установки, строгий whitelist usage/record fields; нет global logging handler/payload retention. In-memory только; restart обнуляет, Soniox WS/UI журнала НЕ подключены, это не вся спецификация persistent logs.
- Existing malformed-response test выявил реальный RED HTTP200+error→успех без текста. `gateways/asr.py::_json` теперь отказывает с sanitized ProviderError. Test отсутствующей local dependency прежде зависел от окружения и при первом прогоне действительно выполнил установленный local Whisper над fixture WAV (не речь/качество, без API/микрофона); теперь explicit sys.modules numpy=None исключает inference. Добавлены4HTTP tests: whitelist usage, simultaneous installation isolation, HTTP200 error privacy OpenAI/compatible.
- Финальная команда из backend: `.venv/bin/python -m pytest tests/test_activity_log_api.py tests/test_chat_retry.py tests/test_asr_error_privacy.py -q --tb=short` →103passed (activity24). Затем Ruff и `.venv/bin/mypy --python-version 3.12 --follow-imports=silent` по src/audiohelper/{activity,config,runtime,app,gateways/asr_activity,gateways/asr,gateways/chat,routes/logs}.py + tests/test_activity_log_api.py →passed9files. Decorator return type исправлен на Coroutine; compatible fixture получил обязательный base_url.
- Полный root `backend/.venv/bin/python -m pytest backend/tests -q --tb=short > .runtime/soniox-migration/activity-backend-full.log 2>&1` →690passed/35failed/2warnings, exit1. ВСЕ35failures — прежний test_languages_api,404 на отсутствующем `/languages`. Сбор тестов исправлен, полная регрессия НЕ принята. Диагностический начальный прогон без activity был666passed/35failed; финальный НИЧЕГО не исключает.
- Root `npm test > .runtime/soniox-migration/activity-frontend.log 2>&1 && npm run typecheck && npm run build && npx playwright test scripts/native-live.smoke.spec.ts scripts/save-on-quit.smoke.spec.ts` →399passed/44files,types/build passed,4Electron smoke passed. Frontend не менялся. Self-review standards/spec самостоятельно: fixed shape/counter whitelist/context isolation/auth/error privacy, нет скрытого skip или константы-заглушки. Нет независимого review по запрету помощников.
- STATUS/API обновлены. Следующий шаг: разрешить старый языковой contract относительно Soniox migration (35failures), затем прежний UI live translation/оригинал/mode selector; он НЕ реализован этим срезом. Канбан env/card отсутствует, root/main dirty tree сохранён; graph2026-09-08 stale/not_tracked → исходники, без reindex. Без внешних API/микрофона/помощников/commit/push. Попытка сохранить generic lesson в diagnosing-bugs skill отклонена tool: skill not found in active profile; другой профиль не менялся.

## ASTRA: live translation projection — проверенный backend-срез
- `native_translation.py::project_live_translation` + `LiveStore.snapshot.live_translation_projection`: final prefix + заменяемый mixed tail по каждому connection, snapshot-local tail IDs (не источники), проверка покрытия/метаданных historical tail. `display_token_ids` сохраняет none/translation порядок внутри turn. Final-only контракт сохранён с additive display field.
- Новый storage regression в `test_native_translation_projection.py`: RED missing live_translation_projection → GREEN; смена provisional speaker2→1 объединяет turns, replay/reopen/очистка хвоста, immutable originals/final projection. Дополнены historical/unknown/orphan/interrupted/request isolation. Новый `test_native_translation_tail_api.py`: production HTTP/WS→gateway/storage, внешний fixture socket; mixed none+перевод внутри одного turn, tail replacement→final без дублей, Pause/новые номера, source isolation/restart.3translation files →14passed/2warnings.
- Финальная backend команда —22files: полный список21files из следующего used-languages среза + `backend/tests/test_native_translation_tail_api.py`, `-q --tb=short` →192passed/2dependency warnings. Из backend `.venv/bin/ruff check` и `.venv/bin/mypy --python-version 3.12 --follow-imports=silent` по src/audiohelper/{native_translation,live_store}.py и tests/{test_native_translation_projection,test_native_translation_tail_api}.py →passed4files;2long-lines исправлены перед финальным прогоном.
- Root `npm test > .runtime/soniox-migration/live-projection-frontend.log 2>&1 && npm run typecheck && npm run build && npx playwright test scripts/native-live.smoke.spec.ts scripts/save-on-quit.smoke.spec.ts` →399passed/44files, types/build passed,4Electron smoke passed. Frontend не менялся. Full backend `.venv/bin/python -m pytest tests -q --tb=short` →exit2, collection ImportError test_activity_log_api missing gateways.asr.OPERATION; `.runtime/soniox-migration/live-projection-backend-full.log`. Полная backend-регрессия НЕ принята.
- Self-review standards/spec самостоятельно: per-request порядок/identity, read-only/no trusted tail/no timing translation, сохранение historical/unassigned arrays. Ограничения: полный дублирующий snapshot, UI не потребляет проекцию, tail IDs не durable; archived segments fallback/раскрытие оригинала/source navigation и режимы впереди. Scoped diff-check passed; untracked runtime проверены реальными исходниками/tests/lint. Read_file ошибочно dedup-нул File not found после смены cwd через execute_code; нужные файлы прочитаны pathlib, cwd возвращён root. Следующий шаг — UI-подключение, НЕ новая backend-only привязка.
- Official Soniox real-time translation docs и live transcription browser проверены: replaceable non-final, chunks не1:1. Graph generation2026-09-08/not_tracked → прямые исходники. Kanban task/env отсутствует, root/main dirty tree сохранён. Самостоятельно, без API/микрофона/помощников/commit/push.

## ASTRA: used languages + final translation projection — проверенный срез
- Выполнены дополнения: Saving locally убрано с pending-only feed; ошибки/retry сохранены. `UsedLanguages.tsx/usedLanguages.css`, SettingsPanel, `store.startRecording`: dropdown1..all, обязательный сохранённый выбор до capture/session creation, старые RU/EN fragment switches удалены. Каталог один на backend (`languages.py`, GET settings supported_languages); used_languages null означает отсутствие выбора, omission/null update сохраняет, invalid422 atomic.
- `schemas/settings_store/runtime`, `migrations native_languages_v6`, `LiveStore.open/snapshot`, `routes.live`, `NativeStream._run`, `SonioxConfig/Gateway.open`: immutable languages на запись, сохранение при restart/Pause/rotation, strict hints. Старые settings/записи не получают выдуманный RU/EN. Прямой legacy WS с unset preferences оставлен совместимым (null/auto); обязательный onboarding в desktop. Official Soniox docs подтверждают best-effort, identification не выключается.
- Продолжена прежняя работа: новый `native_translation.project_final_translation` и `LiveStore.snapshot.final_translation_projection`. Final original turns A/B/A, chunk ownership только для одного известного turn с совпадающим speaker; none passthrough. Missing/partial historic ordered IDs, multi-turn chunk, unknown/different speaker → unassigned, без zip/word alignment/timestamps. Массивы/source IDs/ассистент неизменны. Partial не включён; UI проекцию ещё НЕ использует.
- RED missing final_translation_projection2→GREEN. HTTP WS→gateway→snapshot→restart tests A/B/A unequal counts и split events;6новых persistence tests ambiguous/unknown/different/historical/partial-history/multiple chunks+partial/replay/reopen.10targeted passed. Language RED settings/provider/UI подтверждены выше в истории. `test_native_recording_config/preferences/native_soniox_ws` расширены settings restart, v6 migration, immutable delayed activation/rotation.
- Финальная backend команда из root: `backend/.venv/bin/python -m pytest backend/tests/test_native_translation_projection.py backend/tests/test_used_languages.py backend/tests/test_native_recording_preferences.py backend/tests/test_native_recording_config.py backend/tests/test_native_stream_order_store.py backend/tests/test_native_translation_order_api.py backend/tests/test_native_monologues_api.py backend/tests/test_native_live_store.py backend/tests/test_native_live_api.py backend/tests/test_native_storage_lifecycle.py backend/tests/test_native_capture_boundaries.py backend/tests/test_native_soniox_ws.py backend/tests/test_native_soniox_failures.py backend/tests/test_native_live_ws.py backend/tests/test_native_live_ws_errors.py backend/tests/test_native_ws_server_limits.py backend/tests/test_soniox_gateway.py backend/tests/test_sessions_api.py backend/tests/test_audio_playback_api.py backend/tests/test_audio_ingestion.py backend/tests/test_settings_keys.py -q --tb=short` →186passed/2dependency warnings.
- Из backend `.venv/bin/ruff check` и `.venv/bin/mypy --python-version 3.12 --follow-imports=silent` по16файлам: src/audiohelper/{languages,schemas,settings_store,runtime,gateways/soniox,migrations,live_store,native_stream,routes/live,native_translation}.py и tests/{test_used_languages,test_native_recording_config,test_native_recording_preferences,test_native_soniox_ws,test_native_translation_order_api,test_native_translation_projection}.py →passed. Найденные2long-lines/RUF005/tuple inference исправлены и перепроверены.
- `npm test > .runtime/soniox-migration/used-languages-frontend.log 2>&1 && npm run typecheck && npm run build && npx playwright test scripts/native-live.smoke.spec.ts scripts/save-on-quit.smoke.spec.ts` →399passed/44files,types/build passed,4smoke passed. Native smoke проверяет onboarding, dropdown→real settings→immutable snapshot; screenshot used-languages.png просмотрен, без наложений. Quit fixtures теперь явно выбирают языки, сохраняют прежние PCM/lifecycle assertions.
- Full collect-only →695collected/1error missing gateways.asr.OPERATION; used-languages-backend-collection.log. Полная backend регрессия НЕ принята. Scoped diff-check passed. Самостоятельный standards/spec review по изменённым исходникам/патчам; независимых помощников не было. STATUS/API/clean-ui/recording-config/translation-storage docs обновлены.
- Следующий шаг: provisional mixed-tail projection и сохранённый порядок display внутри turn (none+translation), ambiguous/historical UI без скрытия текста, перевод-primary+раскрытие оригинала+source navigation; затем pre-start mode/target selector. Не выдавать final-only backend за готовый live UI. Snapshot всё ещё полный — long-session не принят. Без реальных API/микрофона, commit/push; Kanban env/card отсутствует, root/main dirty tree сохранён.

## ASTRA: дополнения пользователя — работа продолжается
- Убрано штатное Saving locally и пустой feed при pending-only; ошибки/overflow/retry не изменены. RecorderBar test RED (видимая надпись) → GREEN13tests; `npm run typecheck` passed.
- Следующий срез: used_languages (выбор до первой записи, несколько языков из полного списка), immutable snapshot записи и `language_hints` + `language_hints_strict=true` в Soniox. Official language-restrictions docs подтверждают best-effort, НЕ абсолютный запрет других языков; identification технически остаётся активным. Привязка перевода/раскрытие оригинала ещё впереди.
- Kanban task/env отсутствует. Root/main dirty tree сохранён; graph2026-09-08 stale/not_tracked, исходники прочитаны напрямую. Без API/микрофона/помощников/commit/push.
- Языковой вертикальный срез: `languages.py` единый documented catalog, Settings/StoredSettings partial validated used_languages, v6 immutable recording JSON→snapshot, gateway strict hints. UI dropdown `UsedLanguages`, store start guard до создания сессии/микрофона; старые RU/EN fragment switches удалены. RED settings KeyError→GREEN27; RED provider missing hints→GREEN18; UI RED2→GREEN30.
- Промежуточная полная проверка:20-file native backend команда→180passed/2warnings; npm test→399passed/44files; typecheck/build/4Electron smoke passed. Ruff/strict mypy diagnostic3.12/follow-imports=silent11files passed. Screenshot used-languages.png просмотрен: список внутри границ, отдельная прокрутка. Старые native записи сохраняют null (автоопределение), desktop требует выбор для новых запусков; прямой legacy WS пока допускает null. Следом review миграции/rotation и контракт, затем прежняя работа по переводу.

## ASTRA: mixed token order — проверенный gateway/storage срез
- Gateway `SonioxTokenRef/SonioxEvent.token_order` сохраняет provider order и original/none без добавления timing переводам. Migration v5 + `native_tokens.persist_tokens/read_tokens`, `LiveStore.snapshot` сохраняют/отдают final_stream_tokens и replaceable partial_stream_tokens; старые массивы/IDs/источники неизменны, исторический порядок не выдумывается.
- Новые `test_native_translation_order_api.py` HTTP/WS→restart A/B/A с unequal token counts и none; `test_native_stream_order_store.py` tail replacement/reopen/replay/pre-v5 migration. RED missing final_stream_tokens → GREEN; RED speaker numbers2,1 → GREEN (номера теперь по mixed first appearance). 4-file команда этих2tests + test_native_live_store.py + test_soniox_gateway.py →47passed/2warnings. Ruff6files passed; mypy потребовал list annotations в native_tokens, исправлено, повтор впереди.
- Финальная19-file native backend команда (17files предыдущего среза +2newtests) →170passed/2dependency warnings. API test дополнен разбиением original/translation по3provider responses; recording_config regression проверяет ordered fields через Pause/restart/новые номера. Ruff + strict mypy diagnostic3.12/follow-imports=silent7files passed после list annotations.
- Root `npm test > .runtime/soniox-migration/stream-order-frontend.log 2>&1 && npm run typecheck && npm run build && npx playwright test scripts/native-live.smoke.spec.ts scripts/save-on-quit.smoke.spec.ts` →397passed/44files, types/build passed,4smoke passed. Full collect-only →679collected/1error missing gateways.asr.OPERATION; stream-order-backend-collection.log. Полная backend регрессия не принята.
- Самостоятельный standards/spec review: сохранены source IDs, original-only ассистент, immutable mode/consent; нет fake translation timestamps/zip alignment, historical mixed order не реконструируется. Устранён порядок назначения новых speaker numbers по раздельным массивам. Прочитаны актуальные official rt-translation/stt-translation docs; реальных вызовов не было. API/STATUS/translation-storage contract обновлены. Scoped diff-check passed; runtime untracked исходники проверены tests/lint, git diff не включает их.
- Следующий срез — осторожная проекция mixed chunks на монологи с явным отказом от неоднозначного соответствия, историческим fallback/покрытием IDs; затем перевод-primary/раскрытие оригинала и UI выбора режимов. Текущий срез сохраняет evidence, ещё НЕ реализует эту связь/UI. Snapshot полный с дублирующими проекциями — не long-session-ready. Kanban env/card отсутствует; graph2026-09-08 not_tracked, исходники прочитаны напрямую. Без API/микрофона/помощников/commit/push.

## ASTRA: per-recording config — проверенный API/transport срез
- `migrations.py` additive v4, `live_store.py::open/LiveConnection`: режим/язык фиксируются при первом native open, existing запись сохраняет значения независимо от preferences. `routes/live.py` сериализует settings snapshot/open/key lookup с consent update; audio_only не читает ключ. `native_stream.py` audio_only без worker, translation target из записи в SonioxConfig.
- Новый `test_native_recording_config.py`: RED connecting вместо disabled → GREEN; RED missing translation config → GREEN. HTTP/WS + внешний fixture socket, pause/restart всех режимов, original/translation HTTP storage. Команда `backend/.venv/bin/python -m pytest backend/tests/test_native_recording_config.py backend/tests/test_native_capture_boundaries.py backend/tests/test_native_storage_lifecycle.py -q --tb=short; npm run typecheck` →17passed/2dependency warnings, TypeScript passed.
- Финальная backend regression:17-file native команда (16files preferences этапа ниже + test_native_recording_config.py) →166passed/2dependency warnings. Включены migration v3→v4/reopen без смены режима существующих записей, delayed activation/rotation после изменения settings, transcript-only без translation config. Ruff + strict mypy diagnostic3.12/follow-imports=silent по live_store/native_stream/routes.live/migrations/test_native_recording_config/test_native_soniox_ws →passed6files.
- `npm test > .runtime/soniox-migration/recording-config-frontend.log 2>&1 && npm run typecheck && npm run build && npx playwright test scripts/native-live.smoke.spec.ts scripts/save-on-quit.smoke.spec.ts` →397passed/44files, types/build passed,4smoke passed. Native smoke дополнен audio_only через production preload/main/backend и reload UI: disabled разрешён transport, намеренное отсутствие ASR не показано как ошибка финализации/пропуск. Сохранённые gaps остаются диапазонами без ASR, не доказательством сбоя аудио. Нет нового mode selector.
- Full backend collect-only →675collected/1error, прежний test_activity_log_api missing gateways.asr.OPERATION; лог recording-config-backend-collection.log. Полная backend регрессия не принята. Scoped diff-check passed; untracked runtime проверены исходниками/tests/lint. Self-review standards/spec самостоятельно: конфигурация сохраняется в транзакции первого open, общий settings lock сохраняет consent границу, original остаётся источником, transcription_complete не доказывает готовность перевода. Независимого аудита нет.
- Следующий срез: mixed original/translation chunk order + original-vs-none и связь с A/B/A без ложного1:1 alignment, затем перевод-primary/раскрытие оригинала и UI pre-start выбора. Backend уже потребляет preferences; UI ещё показывает original и не является законченным режимом перевода. Audio-only postprocessing/recovery впереди. Контракт docs/SONIOX-RECORDING-CONFIG.md. Kanban task/env отсутствует; main dirty tree сохранён; graph2026-09-08 stale/not_tracked, исходники прочитаны напрямую. Без API/микрофона/помощников/commit/push.

## ASTRA: persisted recording preferences — проверенный API-срез
- `schemas.py::NativeRecordingMode/Settings/SettingsUpdate`, `settings_store.py::StoredSettings/apply_update`, `runtime.py::settings_view`: сохраняемые `native_recording_mode` (transcription/translation/audio_only, default transcription) и `translation_target_language` (default ru). Это предпочтения следующей записи, НЕ конфигурация существующей сессии. Omission/null сохраняют значение; язык не сбрасывается при переключении режима; invalid update атомарно отклоняется422. Формат языка согласован с текущим SonioxConfig, не является проверкой поддержки языка провайдером.
- `test_native_recording_preferences.py`: публичные GET/PUT, restart всех трёх режимов, старый settings JSON, сохранение языка, отсутствие implicit consent/HTTP calls, malformed input. RED missing native_recording_mode → GREEN. `frontend/src/api/types.ts` и `client.test.ts`: типизированный partial IPC contract, без изменения Recorder/UI.
- Проверка из root `backend/.venv/bin/python -m pytest backend/tests/test_native_recording_preferences.py backend/tests/test_settings_keys.py backend/tests/test_native_capture_boundaries.py -q --tb=short` →31passed,2dependency warnings; `npm test -- frontend/src/api/client.test.ts` →18passed; `npm run typecheck` passed. Из backend Ruff и strict mypy `--python-version 3.12 --follow-imports=silent` поschemas/settings_store/runtime/newtest →passed4files.
- Финальный native backend regression:15-file команда из предыдущего translation среза + `backend/tests/test_native_recording_preferences.py` →161passed,2dependency warnings. `npm test > .runtime/soniox-migration/preferences-frontend.log 2>&1 && npm run typecheck && npm run build && npx playwright test scripts/native-live.smoke.spec.ts scripts/save-on-quit.smoke.spec.ts` →396passed/44files, types/build passed,4smoke passed. `backend/.venv/bin/python -m pytest backend/tests --collect-only -q --tb=short > .runtime/soniox-migration/preferences-backend-collection.log 2>&1` →670collected/1error (exit2), прежний missing gateways.asr.OPERATION. Полная backend регрессия не принята.
- Самостоятельный review standards/spec: сохранены существующие dirty изменения, настройки не меняют consent/профили/ASR generation, old JSON defaults, отдельное имя от legacy SessionMode, UI не обещает неподключённые режимы. Проверен scoped diff-check. Это не независимый аудит и не реализация всего следующего UI-среза.
- Никакого нового управления записью не показано: нельзя выдавать сохраняемый выбор audio_only за запрет Soniox, пока NativeStream не подключён. Следующий обязательный срез — immutable per-recording config при первом open, сохранение через Pause/Resume/reconnect/restart, audio_only без key lookup/socket, translation config→WS→HTTP. Затем mixed chunk order/original-vs-none и монологи с раскрытием оригинала. Нельзя строить A/B/A alignment по раздельным массивам/номеру спикера.
- Kanban env/card отсутствует. Граф2026-09-08 stale, определения проверены исходниками. Самостоятельно, без API/микрофона/помощников/commit/push. Общая миграция не завершена.

## ASTRA: перевод — локально проверенный gateway/storage срез
- `gateways/soniox.py`: optional one_way target config; отдельный `SonioxTranslationToken` без timestamps, final/partial translation отдельно от original. Текущий NativeStream не включает перевод: persisted режим/язык и UI ещё не подключены. Официальный token contract проверен web + browser; источники/перевод не 1:1.
- `migrations.py`: additive native_translation_v3; `native_tokens.py` сохраняет final translation и replaceable translation tail с connection-scoped speaker_number; `live_store.py` отдаёт отдельные snapshot поля, не создаёт translation segments/FTS/фиктивные audio links. Original-only replay digest сохранён совместимым.
- RED gateway TypeError target config → GREEN; RED storage missing partial_translation_tokens → GREEN. Финальный повтор15-file backend команды из первого среза ниже → **144passed,2dependency warnings**. После последней test-only разбивки строки `backend/.venv/bin/python -m pytest backend/tests/test_native_live_store.py backend/tests/test_soniox_gateway.py -q --tb=short` → **44passed**. Проверены negative metadata/config/unsolicited translation, replaceable partial после DB reopen, final reopen, replay/conflicting replay, original-only источники. Это НЕ полный HTTP→NativeStream translation wiring test.
- Из backend `.venv/bin/ruff check src/audiohelper/gateways/soniox.py src/audiohelper/native_tokens.py src/audiohelper/live_store.py src/audiohelper/migrations.py tests/test_soniox_gateway.py tests/test_native_live_store.py` и `.venv/bin/mypy --python-version 3.12 --follow-imports=silent` с теми же6paths → passed. Root Ruff ошибочно классифицирует audiohelper imports; запускать из backend. Scoped diff-check passed; untracked runtime файлы проверены тестами/lint, не полагаться только на git diff.
- `npm test > .runtime/soniox-migration/translation-frontend.log 2>&1 && npm run typecheck && npm run build && npx playwright test scripts/native-live.smoke.spec.ts scripts/save-on-quit.smoke.spec.ts` → **395passed/44files,types/build passed,4smoke passed**. React act warnings AssistantPanel/ProfileEditor. Full collection из backend `.venv/bin/python -m pytest tests --collect-only -q --tb=short > ../.runtime/soniox-migration/translation-backend-collection.log 2>&1` → **653collected/1error**, прежний missing gateways.asr.OPERATION.
- Самостоятельный standards/spec review: untimed translation отдельным типом, original-only digest совместимость, атомарность/event replay, no translation FTS/фиктивных segment IDs; исправлен mypy tuple union, lint длинных test lines. Незавершённое: сохраняемый порядок смешанных chunks/связь с монологами (никакого доказанного1:1 alignment), original vs none status для same-target language, persisted mode/language и NativeStream/UI wiring. `docs/SONIOX-TRANSLATION-STORAGE.md` фиксирует фактический контракт, STATUS сокращён/обновлён.
- Нет активной Kanban env/card; root/main dirty tree сохранён, graph2026-09-08 not_tracked → прямые исходники. Самостоятельно, без помощников, API/микрофона, commit/push. Ближайшее: persisted mode/language + полноценный translation WS→HTTP→UI срез с сохранением порядка original/translation chunks. Этот срез НЕ включает перевод кнопкой Record и НЕ завершает спецификацию.

## ASTRA: первый реализованный срез чистой ленты Soniox
- `gateways/soniox.py`: включена diarization, валидируется/сохраняется optional speaker. Перевод пока намеренно не включён. `migrations.py`: additive `native_tokens_v2` с таблицами `native_speakers` и `native_token_events`; `native_tokens.py`: token provenance/номера в общей транзакции `LiveStore.save_event`. Snapshot возвращает `final_tokens` (стабильный token ID, segment_id, connection_id, абсолютные sample bounds, speaker_number) и `speakers`. IDs разных соединений не склеиваются. Старые сегменты/источники/FTS не переписаны; прошлые speaker данные не выдумываются.
- `NativeMonologues.tsx`/`nativeMonologues.css`, `api/nativeLive.ts`, `TranscriptView.tsx`: A/B/A монологи, соседние токены одного спикера вместе; актуальный replaceable tail обычным текстом. Без per-monologue таймкодов/кнопок источника. Для нового snapshot штатные Soniox state/draft/gaps находятся в раскрываемой диагностике, реальная ошибка/неполнота — одна зарезервированная строка. Poll остаётся read-only. Прокрутка не перехватывается после ухода вверх; «К текущей речи» возвращает вниз. Citation segment ID сохранён; точное word-range выделение пока не реализовано.
- RED→GREEN: новый production WS→gateway→storage→HTTP тест `test_native_monologues_api.py` (только внешний provider socket fixture) проверяет A/B/A, два подключения с номерами1/2→3/4, absolute samples, replay PCM без дублей, source IDs. `test_native_live_store.py` проверяет token/speaker snapshot после DB reopen. UI IPC-boundary test проверяет3→4→3 монолога при смене tail speaker, отсутствие видимых технических статусов/аудиокнопок и чтение выше после6s без захвата прокрутки.
- Финальные frontend команды: `npm test > .runtime/soniox-migration/clean-ui-frontend.log 2>&1 && npm run typecheck && npm run build && npx playwright test scripts/native-live.smoke.spec.ts scripts/save-on-quit.smoke.spec.ts` → **395passed/44files**, TypeScript/build passed, **4Electron smoke passed**. React act warnings в старых ProfileEditor tests. Native smoke теперь проверяет скрытую по умолчанию диагностику и раскрывает её для проверки исходных gaps; screenshot `.runtime/soniox-migration/soniox-native-status.png` просмотрен, это offline fixture без речи, НЕ визуальная приёмка монологов на реальной ASR.
- Backend из root: `backend/.venv/bin/python -m pytest backend/tests/test_native_monologues_api.py backend/tests/test_native_live_store.py backend/tests/test_native_live_api.py backend/tests/test_native_storage_lifecycle.py backend/tests/test_native_capture_boundaries.py backend/tests/test_native_soniox_ws.py backend/tests/test_native_soniox_failures.py backend/tests/test_native_live_ws.py backend/tests/test_native_live_ws_errors.py backend/tests/test_native_ws_server_limits.py backend/tests/test_soniox_gateway.py backend/tests/test_sessions_api.py backend/tests/test_audio_playback_api.py backend/tests/test_audio_ingestion.py backend/tests/test_settings_keys.py -q --tb=short` → **130passed,2dependency warnings**. Ruff7changed Python files passed; strict mypy diagnostic3.12/follow-imports=silent5files passed. Full collection повторена: **639collected/1error**, прежний `test_activity_log_api.py` импортирует отсутствующий `gateways.asr.OPERATION`; лог `.runtime/soniox-migration/clean-ui-backend-collection.log`.
- Самостоятельный standards/spec review: исправлены individual token time bounds, first-final fallback, чтение источника после поздней загрузки, позиционирование follow button относительно ленты. Архив без token metadata остаётся читаемым, без выдуманной атрибуции. Незавершённое: gateway/storage translation,3recording modes, durable recovery jobs/parallel backfill, speaker editing/undo, общий компактный player вместо diagnostic player, RecorderBar cleanup/low-volume debounce, exact citation spans, immutable edit revisions. Snapshot пока полный на каждый poll — нужна incremental/bounded delivery и long-session проверка перед готовностью. Это первый вертикальный срез, НЕ готовая спецификация целиком.
- Ближайший шаг: типизированные translation tokens без фальшивых timestamps + persisted режим/язык, затем восстановление диапазонов; не добавлять «Возобновить», пока реальная команда восстановления не реализована. Без помощников, реальных API/микрофона, commit/push. Dirty tree не откатывался; граф2026-09-08 устарел, исходники сверены напрямую.

## ASTRA: согласован чистый Soniox UI; проверка API перед реализацией
- Новая `docs/SONIOX-CLEAN-UI-SPEC.md` сохраняет подтверждённое интервью: непрерывные speaker turns, без draft/status/timestamps, три режима, встроенный перевод с раскрытием оригинала, ручной parallel live/backfill, источники, editing/undo, quit/sleep и чистая диагностика.
- Живые официальные страницы проверены browser: translation tokens не имеют timestamps/1:1 alignment; ASR+translation один WS, provider error закрывает оба; async audio translation поддерживается. Speaker IDs между независимыми запросами не имеют подтверждённой гарантии соответствия. До 15 speakers/session. Нельзя обещать автоматическое исправление final или точные word timestamps перевода.
- Пользователь подтвердил отдельные speaker identities/новые номера после reconnect/backfill с ручным объединением вместо склеивания одинаковых provider IDs. Ключ (request_id, provider_speaker_id), общий сохраняемый счётчик отображаемых номеров внутри записи. Runtime не менялся; новая спецификация не реализована, тесты кода не запускались. Следующий шаг — token/source projection + публичные RED/GREEN tests.
- Активной Kanban-карты/env нет; main/dirty tree сохранены. Без помощников, commit/push, реальных API/микрофона. Проверка документации не заменяет API acceptance.

## ASTRA: Soniox settings + native transcript UI — текущий срез
- `schemas.py::Settings/SettingsUpdate`, `runtime.py::settings_view`, `routes/settings.py`: добавлены `soniox_has_api_key` и write-only `soniox_api_key` (SecretStr, исключён из сериализации/repr). Ключ отдельный от legacy ASR, omission/null сохраняет, пустая строка удаляет. Keychain I/O off-loop, ошибки санитизированы; удаление закрывает active provider/handshake, даже если последующая settings DB запись отказала. Cloud consent больше не выводится UI из наличия cloud-профилей.
- `SettingsPanel.tsx`, новый `SonioxCredentials.tsx`, wire types: masked input, pending/remove/save/error, очистка после успеха, явное согласие с предупреждением о передаче/оплате; хранение ключа не означает реальную проверку API. Новые public UI→real store/client→IPC tests в `SettingsPanel.soniox.test.tsx`; старые auto-consent ожидания мигрированы.
- `api/nativeLive.ts::NativeSnapshot`, `ApiClient.getNativeSnapshot`, новый read-only `useNativeTranscript.ts`, `NativeTranscriptStatus.tsx`, `TranscriptView.tsx`: чтение final + persisted draft + Soniox state + saved sample watermark + incomplete/gaps. Draft отдельно и не передаётся в Q&A/notes. Таймкоды draft смещаются на start_sample подключения; final сохраняют playback links. Poll последовательный, 1s после ответа, продолжается до inactive после Pause/Stop, cleanup/session-switch не применяет поздний ответ; inactive требует финального повторного GET detail. Никаких ASR-команд из чтения. В этом срезе legacy модели/режимы ещё видны и требуют последующего удаления; read-model не является реализацией edit/revisions/translation.
- Проверки: backend native/storage/settings regression **146passed/2dependency warnings**, scoped Ruff/mypy5files passed. Full frontend/typecheck/build прошли; точный count в `.runtime/soniox-migration/soniox-ui-final-frontend.log` (перед последними2tests было392). Новые native UI tests покрывают replacing draft/final, shared clock, error/stale disclosure, late response after session switch, final arriving during Stop; source playback assertion теперь отдельно считает manifest GET, допускает read-only native polling.
- Production smoke расширен UI assertions и screenshot в `scripts/native-live.smoke.spec.ts`: settings consent remains false через реальный backend; после fixture PCM+pause/resume и reload UI должен показать offline state/gaps.3save-on-quit smoke passed; native smoke пока упал на locator (сначала два одинаковых текста, затем exact accessible name с динамическим suffix). Исправлен locator на role button с префиксом заголовка; повторить. Снимок settings уже создан `.runtime/soniox-migration/soniox-settings.png`; native screenshot ещё не подтверждён.
- Пользователь «продолжай» разрешил продолжение: full backend collection выполнена → **634collected/1error**, `test_activity_log_api.py` импортирует отсутствующий `gateways.asr.OPERATION`. Не исправляли/не удаляли этот blocker; это не новый отказ native UI. Scoped diff-check passed. Self-review самостоятельно, без помощников; реальные API/микрофон/commit/push не запускались. Следом закончить production smoke, визуально проверить screenshots, обновить STATUS/API docs; полный lifecycle review/прочие этапы релиза ещё не приняты.

## ASTRA: последняя backend consent/lifecycle правка — проверенный срез
- `backend/tests/test_native_storage_lifecycle.py::test_transcript_storage_failure_closes_idle_intake_without_next_packet`: timeout вызван некорректным сетевым fixture, отсутствовали обязательные Soniox `final_audio_proc_ms`/`total_audio_proc_ms`. Gateway отклонял событие до записи. Fixture исправлен; реальный SQLite query_only даёт публичный `storage_failed` без следующего пакета. Timeout не увеличен.
- `native_stream.py::disable_provider/wait_failure/offer`: отзыв consent завершает cloud worker/handshake, не отменяет локальный intake; новые PCM не отправляются после revoke. Исправлена Optional-проверка current_task для mypy. `routes/live.py` и `runtime.py::native_settings_lock`: native open согласован с изменением consent. `routes/settings.py::_update_settings`: read/validate/write сериализованы с open; save+revoke полностью защищены drain_on_cancel, включая отмену сразу после commit.
- `test_native_capture_boundaries.py`: HTTP/WS coverage revoke при connecting/streaming, PCM до/после revoke, повторный revoke/regrant без воскрешения cloud worker, отмена HTTP после реального SQLite commit, конкурентное несвязанное обновление настроек. Подменены provider socket и задержка возврата DB write, не результат записи/ACK.
- Команда `backend/.venv/bin/python -m pytest backend/tests/test_native_capture_boundaries.py backend/tests/test_native_storage_lifecycle.py backend/tests/test_native_soniox_ws.py backend/tests/test_native_soniox_failures.py backend/tests/test_native_live_ws.py backend/tests/test_native_live_ws_errors.py backend/tests/test_native_live_store.py backend/tests/test_native_live_api.py backend/tests/test_soniox_gateway.py backend/tests/test_sessions_api.py backend/tests/test_audio_playback_api.py backend/tests/test_audio_ingestion.py backend/tests/test_native_ws_server_limits.py backend/tests/test_settings_keys.py -q --tb=short` → **125 passed, 2 dependency warnings**. Scoped Ruff/mypy финально прошли по6файлам. После восстановления защиты повтор `tests/test_native_capture_boundaries.py tests/test_native_storage_lifecycle.py` →12passed. Full `npm test` → **386passed/42files**, exit0; лог `.runtime/soniox-migration/final-frontend.log`. Typecheck/build/4Electron smoke ранее прошли на предыдущем frontend-срезе; в этом backend-срезе не повторялись.
- Команда `git diff --check -- backend/src/audiohelper/routes/settings.py backend/src/audiohelper/runtime.py backend/src/audiohelper/routes/live.py docs/HANDOFF.md; backend/.venv/bin/python -m pytest backend/tests --collect-only -q --tb=short > .runtime/soniox-migration/final-backend-collection.log 2>&1; ...` **не выполнялась**: approval timeout, exit-1. Не повторять/не обходить без нового разрешения. Полная backend collection сейчас не подтверждена; прежний blocker `test_activity_log_api.py`/`gateways.asr.OPERATION` известен только по предыдущим прогонам. Это не блокирует подтверждённые125targeted тестов, но полный lifecycle review не принят.
- Mutation check: временное снятие drain_on_cancel с save+revoke → cancel-request regression падает на `socket.closed == False`; защита восстановлена в finally. Подтверждено, что тест ловит реальный незакрытый provider после durable consent=false.
- Soniox UI/native snapshot/key/status/gaps остаются отдельным незавершённым этапом. Последняя просьба пользователя — завершить именно эту правку; UI не расширяем в этом срезе. Реальные API/микрофон/помощники/commit/push не запускались. Kanban task/env отсутствует.

## ASTRA: lifecycle тесты перенесены на native — проверенный срез
- `store.lifecycle.test.ts`: снят внутренний mock AudioRecorder; lifecycle-инварианты перенесены в `store.native.test.ts` с production recorder/writer/queue и внешними browser/IPC fixtures. Остались12archive/session isolation tests; автоматическое ASR архива заменено запретом сети по спецификации. Соответствие старых проверок: `docs/NATIVE-LIFECYCLE-TEST-MIGRATION.md`. Slow ASR/consent теперь проверяются на backend-owned HTTP/WS seam:10tests passed (2warnings).
- `store.native.test.ts` теперь25tests: дополнительно startup open/worklet failure cleanup, deferred Pause/retry→failed Stop, session isolation, tail durable barrier, failed Stop+Quit/retry, sticky incomplete, storage refusal до commit и lost ACK. `store.ts`: listener снимается после durable Stop и failed open; открытый транспорт закрывается при browser setup failure; cancelled start не перезаписывает lifecycle Stop. RED cleanup2fail→GREEN.
- Исправлена прежняя ошибка самого polling-теста `LocalModelsBrowser.test.tsx`: fake clock включался ПОСЛЕ создания real timer. Теперь до expansion, fireEvent.click+act; production polling не менялся. Промежуточный userEvent+fake clock дал timeout, заменён fireEvent, затем GREEN.
- Последняя команда `npm test -- frontend/src/components/settings/LocalModelsBrowser.test.tsx frontend/src/state/store.native.test.ts frontend/src/state/store.lifecycle.test.ts && npm run typecheck && npm run build && npx playwright test scripts/save-on-quit.smoke.spec.ts scripts/native-live.smoke.spec.ts` →53passed,types/build exit0,4smoke passed. Full frontend перед polling fix и последним новым native test:384passed/1polling failure, без unhandled, React act warnings. Full после последних правок ещё повторить.
- Soniox UI/key/native snapshot и active consent revoke ещё НЕ реализованы. Реальные API разрешены пользователем только после окончательной локальной проверки; пока НЕ запускались. Без микрофона/помощников/commit/push. Read_file ошибочно закешировал File not found после cwd backend; нужные source прочитаны через pathlib (реальные файлы существуют), cwd возвращён root.

## ASTRA: native lifecycle cleanup — проверенный промежуточный срез
- Пользователь разрешил тестовые реальные API-запросы ПОСЛЕ окончательной локальной проверки. Это новое разрешение уточняет прежнее «API только пользователь»; физический микрофон отдельно не разрешён. API пока не запускались, финальная локальная проверка ещё не пройдена.
- Свежий baseline `npm test -- frontend/src/state/store.lifecycle.test.ts frontend/src/state/store.native.test.ts` →15passed/23failed/2unhandled: прежние lifecycle fixtures без onNativeFailure, legacy expectations. Ничего из старых проверок не удалено; миграция впереди.
- `store.native.test.ts`: добавлены реальные store/recorder/writer сценарии через browser+IPC boundaries: Quit во время open, late failure/unsubscribe после Stop, failed Pause+retry с переключённым selected session, повторный Stop, Stop во время Resume/open. `store.ts::acknowledgeSessionStatus` снимает listener после durable Stop; callback игнорирует позднее событие. Writer оставлен для идемпотентного повторного Stop. RED unsubscribe0calls → GREEN.
- Финальная команда среза `npm test -- frontend/src/state/store.native.test.ts frontend/src/audio/nativeWriter.test.ts && npm run typecheck && npm run build && npx playwright test scripts/save-on-quit.smoke.spec.ts scripts/native-live.smoke.spec.ts` →12passed, types/build exit0,4Electron smoke passed. Smoke изолированный, fixture PCM, без API/микрофона. Python не менялся.
- Дальше: миграция старых lifecycle-инвариантов на native browser/IPC; failed open/start cleanup, Soniox UI и consent revoke; full regression/review. Активной Kanban-карты/env нет, root/main подтверждён, graph2026-09-08 stale/not_tracked → актуальные исходники. Без помощников/commit/push.

## ASTRA: разрешение получено — native Record smoke подтверждён, regression красный
- Пользователь «разрешаю» снял approval blocker предыдущего среза. Разрешённая команда выполнена: targeted6passed, build exit0,4Electron smoke passed. Native Record теперь проверен через production store/recorder/main/backend с browser PCM fixture и точным playback после restart; без реального микрофона/API.
- Дополнительный полный `npm test` →362passed/24failed,2unhandled rejections. Лог `.runtime/soniox-migration/native-record-frontend.log`. Из24failures23в store.lifecycle.test.ts (старый fixture без onNativeFailure, legacy PATCH/WAV/ASR/discovery expectations),1прежний LocalModelsBrowser polling. Это текущая незавершённая миграция тестов, НЕ зелёная регрессия. Не удалять проверки ради pass: перенести их lifecycle-инварианты на native IPC/browser boundaries.
- Self-review выявил реальную ошибку: успешный durable native Stop ACK ошибочно становился pendingSessionStatus при failed GET refresh, затем retry пытался открыть уже stopped writer. Новый store.native тест RED pending='stopped' →GREEN: acknowledgeSessionStatus теперь принимает durable native ACK без повторного GET; read-model refresh не отменяет сохранение.
- Финальная команда этого этапа `npm test -- frontend/src/state/store.native.test.ts frontend/src/audio/nativeWriter.test.ts && npm run typecheck && npm run build && npx playwright test scripts/save-on-quit.smoke.spec.ts scripts/native-live.smoke.spec.ts` →7passed,TypeScript/build exit0,4smoke passed. Полный frontend после последнего fix не повторён; Python не изменялся/не перепроверялся.
- Следующий шаг — миграция оставшихся lifecycle fixtures/инвариантов и проверка отмены/open/retry/cleanup, затем Soniox key+native transcript/status/gaps UI и consent revoke. Полный lifecycle review/миграция НЕ завершены. Активного approval blocker больше нет; предыдущая запись ниже историческая. Без помощников/commit/push.

## ASTRA: Native Record wiring — незавершённый срез, verification BLOCKED approval
- Пользователь попросил довести миграцию до lifecycle review. Активной Kanban-карточки нет: kanban_show missing task_id, workspace env отсутствует. root/main проверен, существующий dirty tree сохранён. Graph generation2026-09-08 stale/not_tracked; использованы актуальные исходники.
- `frontend/src/audio/nativeWriter.ts`: ошибка open теперь переводит writer в failed, чтобы finish не объявлял неподтверждённое открытие завершённым. `nativeWriter.test.ts` новый RED (finish resolved) → GREEN3; отдельный typecheck passed до store wiring.
- `frontend/src/state/store.ts`: начато переключение новых Record на NativeAudioWriter; onReady/open до capture,100ms windows, maxPending8+2protected tail slots, maxAttempts1 и explicit reopen/retry; Pause/Stop через native finish; Resume открывает transport прежде capture; native failure останавливает capture. Удалён автоматический legacy transcription discovery при открытии записей. Архивный contextual UI/backend ещё остаётся, новые записи используют compatibility mode legacy с native provenance. Это НЕ завершённая миграция.
- Новый `frontend/src/state/store.native.test.ts`: real store/recorder/writer + внешние browser/IPC fixtures, RED missing open → GREEN1 (100ms PCM + точный16sample tail + Quit). После устранения промежуточных type/syntax ошибок `npm run typecheck; npm test -- frontend/src/state/store.native.test.ts` →exit0/1passed. Затем добавлены два теста Pause/Resume и lost ACK explicit retry — ОНИ ЕЩЁ НЕ ЗАПУЩЕНЫ.
- `scripts/save-on-quit.smoke.spec.ts`: restart verification теперь собирает PCM всех manifest chunks, а не только sequence0 (при100ms исходные1600samples и16sample tail — разные блоки). Изменённый smoke ЕЩЁ НЕ ЗАПУЩЕН.
- **БЛОКЕР:** команда `npm test -- frontend/src/state/store.native.test.ts frontend/src/audio/nativeWriter.test.ts && npm run build && npx playwright test scripts/save-on-quit.smoke.spec.ts scripts/native-live.smoke.spec.ts` НЕ выполнялась: approval timeout, exit-1, output пустой. Не повторять и не обходить до явного разрешения пользователя. После разрешения сначала эта команда и исправление реальных failures.
- До lifecycle review обязательно: проверить отмену во время native open/start и cleanup, failed end/retry (включая успешный end+failed GET), повтор Stop/Pause гонки, unsubscribe failure callback; обновить старые store.lifecycle тесты под native публичный контракт (сейчас многие всё ещё ожидают legacy PATCH/WAV/ASR и НЕ проверялись после wiring); Soniox key/UI/native snapshot polling/draft/gaps и consent revoke; full suites/lint/types/build/smoke. Ограничение≤1s не доказано одной maxPending настройкой. Runtime store переключён, но текущий UI ещё не показывает native transcript/status — продукт не готов.
- Полный regression и self-review этого среза НЕ выполнены. Не было реальных API/микрофона, помощников, commit/push. При продолжении не считать предыдущие59/118/4smoke доказательством нового store wiring.

## ASTRA: capture timeout fail-closed — проверенный срез
- Финальная проверка: `npm run build && npx playwright test scripts/save-on-quit.smoke.spec.ts scripts/native-live.smoke.spec.ts` →build exit0,4passed. Новый smoke параметризован: acknowledged / quit-timeout / pause-timeout; production capture/store/main/backend, fixture browser input и dialog decision. Stay сохраняет backend/window, повтор Quit и late ACK не обходят отказ; explicit discard→restart→точные1600+16PCM samples и stopped status. Изоляция temp profile + null keyring проверяется до действий.
- Полный `npm test` →381passed/1failed, прежний LocalModelsBrowser.test.tsx:293 polling2expected/1actual; TypeScript отдельно passed, scoped diff-check passed. Backend full pytest →collection error прежний missing gateways.asr.OPERATION. Scoped118backend tests (та же13-file команда, что в предыдущем срезе capture prerequisites) повторены из root с backend/tests prefixes →118passed,2dependency warnings. Python не менялся; Ruff/mypy заново не запускались.
- Self-review по standards/spec текущего среза: проверены timer/waiter cleanup, late ACK, Pause teardown, repeat Stop/Quit и сохранение received tail; добавлена обработка postMessage throw тем же fail-closed путём (production smoke проверяет timeout, не throw). Отдельный sticky флаг нужен, потому что recorderError очищается другими lifecycle действиями. Это не полный migration review и не независимый аудит. Защита полноты пока renderer-owned на текущий запуск; durable per-session capture gap/reload recovery не реализован.
- Ближайший шаг: NativeAudioWriter→Record/store,100ms+bounded queue+retry/reopen и native-vs-legacy discovery; затем Soniox UI. Не считать текущий Record Soniox-ready. STATUS сокращён; исходный STATUS целиком сохранён в docs/STATUS-HISTORY-PRE-CAPTURE-BARRIER.md.

### Промежуточная проверка этого среза
- AudioRecorder.drainCapture/awaitWorkletBarrier: timeout или postMessage failure → отдельный onCaptureIncomplete, сохранение полученного хвоста, teardown (в том числе Pause), невозможность Resume после неизвестного хвоста. Store.captureIncomplete sticky на весь запуск: повтор Quit/другая запись не превращают неизвестную полноту в success; prepareForQuit=false после drain. Pause после forced stop проходит штатный Stop/status ACK.
- RED recorder2→GREEN11; RED store1→GREEN34. `npm test -- frontend/src/audio/recorder.test.ts frontend/src/state/store.lifecycle.test.ts frontend/src/security/saveOnQuit.test.ts frontend/src/security/rendererSaveBarrier.test.ts frontend/src/security/quitController.test.ts` →59passed; `npm run typecheck` →exit0. Production timeout smoke и review ещё выполняются; full regression ещё не повторён.
- Нет активной Kanban-карточки: kanban_show вернул missing task_id, env workspace не задан; работа в подтверждённом root/main. Graph generation2026-09-08 stale/not_tracked; исходники проверены напрямую. Без API/микрофона/помощников/commit/push. Writer/Record и Soniox UI не подключены.

## ASTRA: разрешение получено — production save-on-quit smoke прошёл
- Пользователь «Разрешаю» снял предыдущий approval blocker. `npm run build && npx playwright test scripts/save-on-quit.smoke.spec.ts scripts/native-live.smoke.spec.ts` →build exit0,2passed. Save-on-quit smoke действительно прошёл: production Record/store/recorder + browser PCM fixture, повтор Quit/Close во время barrier, exit/restart, точное сохранённое PCM и stopped status. Native smoke также passed. Временные изолированные профили, null keyring; реального микрофона/API/моделей нет.
- `npm test` →379passed/1failed: прежний LocalModelsBrowser.test.tsx:293 polling2expected/1actual, React act warnings. Последующий `npm run typecheck` отдельно →exit0 (в цепочке && он был пропущен из-за test exit1).
- Scoped Ruff6backend files сначала нашёл import order в новом test_native_capture_boundaries.py; исправлено patch, повтор All checks passed. Полная scoped native/backend команда из предыдущего среза повторена →118passed,2dependency warnings.
- Это проверка успешного quit-пути, не полное принятие lifecycle: capture barrier timeout/fail-closed и полный review ещё остаются. NativeAudioWriter не подключён к Record, native state/gaps/key UI и отзыв consent внутри активного WS не завершены. Без помощников/commit/push. Предыдущий approval blocker ниже — история, не активный запрет.

## ASTRA: save-on-quit подключён, production verification BLOCKED approval
- `electron/quitController.ts`: optional saveBeforeQuit/onCancelQuit, фаза saving блокирует повторные Quit/Close; backend останавливается только после save ACK либо явного discard после отказа. `electron/rendererSaveBarrier.ts` новый main-owned request ID/ACK,30s timeout=false, stale ACK игнорируется, renderer loss=false. `main.ts` + channels/preload/bridge связывают prepareQuit/quitPrepared/cancelQuit, ACK принимает только trusted top-level sender. При отказе default — Stay in SKAZ; renderer lock снимается при отмене.
- `frontend/src/state/store.ts`: prepareForQuit/cancelQuit/quitRequested; Stop→capture barrier→PersistenceQueue.drain→проверка pending lifecycle/unsaved queue. Во время quit запрещены start/resume/newSession. Исправлены поздние старты после Stop на async create/start boundary. **NativeAudioWriter по-прежнему не подключён к Record**, это save-on-quit для текущего store, не завершение native migration.
- RED3→GREEN saveOnQuit.test.ts; RED missing module→GREEN rendererSaveBarrier.test.ts; RED2 missing prepareForQuit→GREEN store.lifecycle. Последняя команда `npm test -- frontend/src/state/store.lifecycle.test.ts frontend/src/security/saveOnQuit.test.ts frontend/src/security/rendererSaveBarrier.test.ts frontend/src/security/quitController.test.ts` →47passed. `npm run typecheck` →exit0. Scoped tracked git diff --check →exit0. Full frontend/backend lint/types, полное review и production verification этого среза ещё НЕ выполнены.
- Новый `scripts/save-on-quit.smoke.spec.ts`: production app/recorder/store/main/backend; browser capture fixture (не реальная речь/микрофон), null keyring, temp profile paths проверяются; ASR cloud profile+consent=false предотвращают запуск модели. Сценарий Record→1600samples→Quit→16queued samples→повтор Quit/Close→barrier→restart→exact PCM и stopped status. **Сценарий написан, НЕ запускался.**
- **БЛОКЕР:** `npm run build && npx playwright test scripts/save-on-quit.smoke.spec.ts scripts/native-live.smoke.spec.ts` tool approval timeout, output пустой/exit-1; команда НЕ выполнилась. Вопрос clarify о разрешении также timed_out без ответа. Не повторять/обходить до явного согласия. После него запустить команду, исправить реальные failures, затем full suites/lint/review. В review отдельно проверить fail-closed при recorder barrier timeout (сейчас prepareForQuit проверяет queue/lifecycle, но не отдельный флаг capture completeness), отказ/renderer crash, security ACK boundary.
- Нет реальных API/микрофона/помощников/commit/push. Native state/gaps, Soniox key UI, store writer wiring, отзыв consent внутри уже активного native WS остаются незавершёнными.

## ASTRA: capture prerequisites — проверенный промежуточный срез
- `live_store.append_audio`: хвост в1сэмпл сохраняется без дополнения/отбрасывания, точная шкала native samples; целочисленные display ms могут совпадать. API regression проверяет playback bytes и resume с1до16samples. `routes/live.receive_live_audio`: cloud_consent проверяется до чтения ключа/создания provider; false сохраняет локально без сети. Проверка пока при open, изменение consent внутри активного подключения ещё требует обработки.
- Новый `backend/tests/test_native_capture_boundaries.py`: RED invalid_stream и consent false→connecting; GREEN3. Native provider fixtures теперь явно выставляют consent=true. `test_native_storage_lifecycle` ждёт публичный streaming перед инъекцией readonly DB: исходный тест нестабилен из-за гонки provider readiness, сначала117pass/1timeout, повтор после barrier118passed.
- Из backend `.venv/bin/python -m pytest tests/test_native_capture_boundaries.py tests/test_native_storage_lifecycle.py tests/test_native_soniox_ws.py tests/test_native_soniox_failures.py tests/test_native_live_ws.py tests/test_native_live_ws_errors.py tests/test_native_live_store.py tests/test_native_live_api.py tests/test_soniox_gateway.py tests/test_sessions_api.py tests/test_audio_playback_api.py tests/test_audio_ingestion.py tests/test_native_ws_server_limits.py -q --tb=short` →118passed,2warnings. Frontend/store/quit ещё не изменены этим срезом. Продолжается интеграция, не готовая миграция. Без API/микрофона/помощников/commit/push.

## ASTRA: capture readiness и renderer writer — проверенный промежуточный срез
- После transport wiring добавлены AudioRecorder.onReady(sampleRate) перед подключением capture и windowSeconds (100ms поддержаны, старый default5s пока сохранён). Исправлен Stop во время незавершённого getUserMedia: позднее разрешение больше не запускает захват. recorder.test.ts RED2→GREEN10tests на browser boundary без микрофона.
- Новый frontend/src/audio/nativeWriter.ts: перевод captured PCM WAV в narrow PCM bridge, sample/sequence clock через Pause/Resume, finalization barrier, explicit reopen с проверкой durable watermark после lost ACK без повторной отправки уже сохранённого блока. nativeWriter.test.ts:2passed через ApiClient/fixture bridge. **Writer пока не подключён к store/кнопке Record.** Это не законченный app lifecycle.
- Последняя повторная проверка: npm test →372passed/1failed (прежний LocalModelsBrowser.test.tsx:293, polling ожидает2calls,получает1), React act warnings. npm run typecheck →pass; npm run build →pass; npx playwright test scripts/native-live.smoke.spec.ts →1passed. Backend-команда115tests из среза ниже повторена →115passed,2dependency warnings. Scoped git diff --check по изменённым tracked-файлам →pass. ws8.21.3/@types/ws8.18.1 подтверждены npm ls --depth=0.
- Следующий шаг: подключить writer к store/PersistenceQueue с100ms capture и bounded retained duration, native stop/retry/reopen, штатный save-on-quit. При интеграции обязательно проверить короткий хвост <1ms (LiveStore сейчас требует≥1ms), cloud_consent перед Soniox и native-vs-legacy discovery при повторном открытии записей. Это замечания review/следующие проверки, не пройденные критерии. UI ключа Soniox, live draft/state/gaps и удаление старых ASR путей также не завершены.
- Изменения этого среза не затрагивали state/store.ts, QuitController и UI настроек. Микрофон, реальные API, помощники, commit/push не запускались. Нельзя называть текущую кнопку Record Soniox-ready.

## ASTRA: main-owned native transport + renderer API — проверенный срез
- Пользователь явно разрешил установку: ws/@types/ws установлены, blocker approval снят. npm audit --omit=dev:0; общий install audit:2moderate (не исправлялись автоматически).
- electron/nativeLive.ts: один main-owned WS, bearer только main, bounded64packets/2sPCM, сериализация до durable ACK,500ms/block,5sopen/save и15send timeout, idempotent end, sanitized failure event и abort при renderer loss. channels/ipc/preload/bridge и ApiClient.openNative/sendNativeAudio/endNative/onNativeFailure подключены. На renderer не передаются URL/port/token.
- RED→GREEN nativeLive.test.ts (новый transport), client.test.ts (отсутствовал api.openNative). Последняя проверка: npm run typecheck; npm test -- frontend/src/security/nativeLive.test.ts frontend/src/api/client.test.ts →21passed; npm run build; npx playwright test scripts/native-live.smoke.spec.ts →1passed.
- Smoke использует production main+preload+реальный изолированный backend, PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring (тип проверен), отдельный temp user-data, без реального API/микрофона. Проверены auth boundary без токена в renderer, durable PCM, pause/resume sample clock, repeated stop и точное playback содержимое. PCM fixture — транспортная проверка, не речь/ASR quality.
- RED Electron smoke выявил build-only проблему: Vite подменял optional bufferutil пустым stub, ws handshake работал, binary PCM падал. В electron.vite.config.ts ws оставлен external dependency, после этого production smoke GREEN.
- UI capture/store ещё НЕ переключены: AudioRecorder/store всё ещё используют старый WAV persistence path. Ближайший срез — короткие PCM пакеты, frontend capture barrier/queue, retry/stop/quit и Soniox UI. Полные тесты пока не повторены. Старые backend115passed — предыдущий проверенный срез ниже. Без помощников/commit/push.


## ASTRA: off-loop native storage и lifecycle — проверенный срез; Electron заблокирован установкой
- Последняя повторная проверка (после всех runtime-правок): из backend `.venv/bin/python -m pytest tests/test_native_storage_lifecycle.py tests/test_native_soniox_ws.py tests/test_native_soniox_failures.py tests/test_native_live_ws.py tests/test_native_live_ws_errors.py tests/test_native_live_store.py tests/test_native_live_api.py tests/test_soniox_gateway.py tests/test_sessions_api.py tests/test_audio_playback_api.py tests/test_audio_ingestion.py tests/test_native_ws_server_limits.py -q --tb=short` → **115passed**,2dependency warnings,exit0. Scoped Ruff native_io/native_stream/routes.live/newtest passed; strict mypy --python-version3.12 --follow-imports=silent те же4files passed. Full suites/TS/build/Electron smoke в этом заходе НЕ повторялись.
- Дополнительно к промежуточному срезу ниже: `NativeStream.wait_failure` и route race с socket.receive немедленно сообщают storage_failed даже без следующего PCM; append/rotation защищены asyncio.Lock. `stream.stopped` теперь после durable session status, освобождает owner до ACK, разрешает немедленный Resume, старый finally не переписывает новый статус. abort закрывает storage в finally; независимая попытка сохранить end_state даже после ошибки worker.
- New tests/test_native_storage_lifecycle.py:5 API regressions (fsync nonblocking, snapshot/delete при stalled fsync, pause status commit barrier + immediate resume, idle transcript storage failure); RED→GREEN подтверждены. Ручной review standards/spec выполнен самостоятельно по текущим правкам: общая миграция не завершена; исправлены allocation owner race, commit/offer/rotation race, cleanup CancelledError, late status и idle storage failure. Общий git diff --check обнаружил прежний trailing whitespace AGENTS.md:15; не менялся.
- **Блокер:** `npm install ws && npm install --save-dev @types/ws` из root остановлен tool approval timeout: пользователь НЕ дал согласия, установка не выполнена. Не повторять/не обходить до явного разрешения. Нужен WS-клиент Electron main с Authorization header; исходники main/ipc/preload/bridge/recorder/store/quitController/backend прочитаны, но НЕ изменены в этом заходе. Следующий шаг после согласия — установить зависимости, реализовать main-owned bounded native transport + narrow preload API, capture/stop/quit barrier, frontend/state/key UI и isolated smoke.
- Ограничения: off-loop только native path и session list/create/detail/native snapshot/delete. Другие legacy async DB callers ещё могут блокировать event loop на shared lock; WAV per block без coalescing/reconciliation. Физически зависший disk I/O нельзя безопасно бросить по timeout: cleanup ждёт его, 10s — сетевой finish, не гарантия полного wall-clock shutdown. Полный app lifecycle/штатный quit/повторный Stop через frontend ещё не принят. Реальные API/микрофон/помощники/commit/push не запускались.

## ASTRA: off-loop storage — промежуточный проверенный срез
- Новое `backend/src/audiohelper/native_io.py`: disk_call/to_thread + drain_on_cancel, отмена ждёт завершения уже начатого I/O. native_stream: append_audio + storage lock для commit/offer/rotation, off-loop сохранение событий/close/snapshot. routes/live резервирует owner до async open; cleanup барьер переживает отмену. routes/sessions off-loop native snapshot/delete; list/create/detail исполняются в FastAPI threadpool.
- `tests/test_native_storage_lifecycle.py`: RED health timeout при fsync, затем ещё 2 RED при snapshot/delete, GREEN. Команда из backend `.venv/bin/python -m pytest tests/test_native_storage_lifecycle.py tests/test_native_soniox_ws.py tests/test_native_soniox_failures.py tests/test_native_live_ws.py tests/test_native_live_ws_errors.py tests/test_sessions_api.py -q --tb=short -x` →31passed,2dependency warnings,exit0. Промежуточные14failures были CancelledError при TestClient exit во время cleanup; исправлен route-owned cancellation boundary.
- Ещё НЕ приняты полный lifecycle, остальные legacy async DB readers при disk lock, немедленное уведомление об ошибке transcript worker, Electron/frontend; полный regression/lint/typecheck впереди. Graph2026-09-08 stale/not_tracked, snippet delete неверный; исходники прочитаны напрямую. Работа самостоятельно, без API/микрофона/помощников/commit/push.

## ASTRA: первый Soniox backend wiring — проверен
- Разрешение на патчи получено. Подключён native_stream.NativeStream из routes/live.receive_live_audio: storage-first offer, bounded send/receive, retry с текущего saved clock, no backfill, final persistence до stopped. live_store.append_audio получил replay_start_sample для local WS replay через ASR rotation; GET snapshot gaps, GET live transcription state.
- Runtime.native_tasks/native_streams + stop_native; routes.sessions.delete_session ждёт освобождения provider перед удалением; app lifespan stop_native до DB close. При первом тесте delete ожидание всего ASGI task зависло120s; исправлено ожиданием route-owned cleanup Event,19tests затем прошли. Gateway SonioxSession.aclose теперь отменяет shielded finish task: RED оставался блокированный END send →GREEN.
- Последняя команда из backend: `.venv/bin/python -m pytest tests/test_native_soniox_ws.py tests/test_native_soniox_failures.py tests/test_native_live_ws.py tests/test_native_live_ws_errors.py tests/test_native_ws_server_limits.py tests/test_native_live_store.py tests/test_native_live_api.py tests/test_soniox_gateway.py tests/test_sessions_api.py tests/test_audio_playback_api.py tests/test_audio_ingestion.py -q` →110passed,2dependency warnings,exit0. Scoped Ruff native_stream/routes.live/live_store/runtime/app/gateway+2newtests passed; strict mypy --python-version3.12 --follow-imports=silent native_stream/routes.live/live_store/runtime/gateway+2newtests passed7files.
- Полный backend повторён: collection error прежний missing gateways.asr.OPERATION в test_activity_log_api. Full frontend363passed/1failed прежний LocalModelsBrowser polling:293; act warnings. npm run typecheck и npm run build отдельно прошли exit0. UI/Electron smoke нового пути НЕ выполнялся.
- Review выполнен самостоятельно по standards/spec; исправлены replay после rotation, duplicate owner race, provider progress beyond submitted PCM, освобождение END task. Новые7API tests: final+duplicate, delayed connect/no backfill/anchor/gap, deletion, stalled connect/send/finish, reconnect. Это не реальные ASR результаты.
- Ограничения: fsync всё ещё на event loop, WAV на каждый блок, нет coalescing/полной reconciliation; GET polling вместо transcript push; состояние ошибки storage worker проверяется при следующем PCM/end; keyring soniox UI не подключён. Lifecycle shutdown cancellation подключена, но штатный Electron quit/barrier не принят;10s finish может иметь дополнительный bounded cleanup. Импорт, gap recovery, перевод/revisions, удаление legacy и приёмка30min впереди. Следующий этап — усилить storage worker/lifecycle и связать Electron capture/IPC/frontend с native WS. Реальных API/микрофона, помощников, commit/push не было.

Ниже история, прежний approval blocker снят.

## ASTRA: Soniox wiring — незавершённый рабочий срез
- Продолжение выполняется самостоятельно. Свежий baseline: `.venv/bin/python -m pytest tests/test_native_live_ws.py tests/test_native_live_ws_errors.py tests/test_native_ws_server_limits.py tests/test_native_live_store.py tests/test_native_live_api.py tests/test_soniox_gateway.py -q` из backend →46 passed,2 dependency warnings.
- Добавлен `backend/tests/test_native_soniox_ws.py`: production WS+gateway с fake внешним socket; RED подтверждён (ожидался connecting, фактически unavailable),1 failed. Реальные API/микрофон не вызывались.
- Добавлен предварительный `backend/src/audiohelper/native_stream.py::NativeStream`: bounded очередь, независимые send/receive, reconnect anchor и finish. Модуль ЕЩЁ НЕ ПОДКЛЮЧЁН и НЕ ПРОВЕРЕН; не считать принятой реализацией. Требуются review, lifecycle shutdown/delete, status/gaps API и негативные тесты. Синхронное storage пока остаётся на event loop.
- Попытка применить wiring-патчи routes/live.py через execute_code заблокирована: approval timeout, tool_calls_made=0. Изменения route НЕ применены; не обходить отказ другим инструментом, дождаться явного разрешения пользователя. Никаких помощников, commit/push.
- Graph generation2026-09-08 устарел; новые модули not_tracked, snippet delete_session неверен; текущие определения проверялись read_file.

## ASTRA: локальный PCM WebSocket — проверен
- Самостоятельно добавлен `backend/src/audiohelper/routes/live.py`, подключён через app.py отдельно от HTTP dependencies. WS `/sessions/{session_id}/live/stream`: JSON open, binary !QQI+PCM, durable ACK, replay/order/limits, stop/pause/offline incomplete. Security local_access_error расширен на HTTPConnection, WS сам проверяет bearer/loopback peer/Host/Origin/session. Soniox forwarding ещё не реализован: честные unavailable/complete=false.
- `__main__.py`: ws_max_size48020, ws_max_queue8, compression off. New tests native_live_ws/native_live_ws_errors/native_ws_server_limits. RED→GREEN endpoint/hello KeyError/server limits/SQLite readonly+cleanup. Полный scoped native WS/store/API+sessions/playback/ingestion/gateway **103passed**,2dependency warnings; Ruff+strict mypy diagnostic3.12 scoped6files passed. Последний diff после103tests только формат строки test; runtime тот же.
- Контракт `docs/SONIOX-LIVE-API.md`. Не было реального API/микрофона, TestClient выполнял production route в temp fixture. TestClient ws defaults testserver даже при loopback base_url — исправлены абсолютные ws URLs, security не ослаблялась.
- Следующий шаг: подключить уже существующий SonioxGateway к storage-first потоку с отдельными bounded send/receive, правильным anchor при reconnect, gaps и shutdown/delete. Текущий route синхронно fsync на event loop (ограничение), нет coalescing/полной архивной reconciliation. Затем Electron/frontend; весь core ещё in_progress. Помощники не запускались.

## ASTRA: native storage — проверен первый срез
- Работа выполняется самостоятельно, без делегации. Новые `backend/src/audiohelper/live_store.py` (open/append_audio/save_event/close/snapshot/recover_interrupted), `migrations.py` (native_live_v1), `audio_storage.py` (shared fsync/F_FULLFSYNC/rename). Изменены db (FULL/fullfsync+migration), runtime (store+startup recovery), repository (native final-writer guard), ingestion (shared storage+legacy intake guard), routes/sessions (GET live snapshot).
- Новые tests/test_native_live_store.py и test_native_live_api.py. RED подтверждены: отсутствующий модуль; old writer не запрещался; отсутствующие recovery/runtime/store/API. GREEN:89 backend native store/API+sessions+playback+ingestion+Soniox tests passed, exit0; scoped Ruff passed; strict mypy5files с diagnostic Python3.12/follow-imports=silent passed. Project target3.11 всё ещё конфликтует с установленными NumPy stubs. Опечатка read dependencies.py не использовалась; реальный helper routes/deps.py.
- `docs/SONIOX-LIVE-STORAGE.md` описывает фактический контракт и ограничения. WebSocket/send/receive Soniox ещё НЕ подключены; запись WAV на каждый<=500ms block, нет коалесцирования/полной архивной reconciliation. Старые данные проверены на legacy DB fixture; это не прогон пользовательской БД. Реальные API/микрофон и удаление моделей не выполнялись.
- Следующий конкретный шаг: native WS route с отдельными auth/origin/session/order/size guards, независимые bounded storage/send/receive задачи, gaps и lifecycle, API-level fake-network тесты; затем Electron/frontend. Core todo остаётся in_progress. Код остановленного SOL core не использовался как принятая реализация.

## Пользователь отменил делегацию
- SOL backend live process `proc_2788f822940d` остановлен через process.kill по прямой команде пользователя. Далее ASTRA работает самостоятельно, без SOL, субагентов и внешних coding-помощников.
- Незавершённые изменения исполнителя не откатывались; перед продолжением проверить текущие файлы. Предыдущие указания о запущенном исполнителе ниже исторические.

## Проверен gateway; начат backend live-срез
- Пользователь разрешил установку: `uv pip install --python backend/.venv/bin/python 'websockets>=15.0,<16'` →websockets15.0.1 installed, exit0. Предыдущая попытка была остановлена по timeout подтверждения и не обходилась.
- Gateway SOL завершил bounded implementation (self-report76tests); parent прочитал gateway/test/smoke и исправил: `_finish_once` deadline включает блокировку END send (новый тест RED timeout→GREEN); smoke хранит результаты по мере событий, дренирует очередь при ошибках и пишет token timestamps (новый тест RED KeyError→GREEN).
- Parent в проектном venv: gateway+provider/privacy+playback/ingestion **127 passed**, exit0. Scoped mypy strict/follow-imports=silent gateway/test/smoke3files passed. Ruff из backend cwd `--config pyproject.toml`5files passed; root cwd давал отличающуюся конфигурацию/isort, это не повторный дефект runtime. TypeScript/build passed. Реальные API/микрофон не запускались; тест smoke --allow-paid-api только с подменённым сетевым connect и тестовым ключом.
- Следующий backend live исполнитель запущен: process `proc_2788f822940d`, requested gpt-5.6-sol medium, command Codex workspace-write. Brief `.runtime/soniox-migration/core-brief.md`; ожидаемые `core-report.md`, `core-last.txt`, `docs/SONIOX-LIVE-API.md`. Scope backend native PCM persistence/WS/auth/ASR lifecycle/stable segments/migration, без frontend/cache deletion/платных вызовов. Parent ещё не принимал этот срез; дождаться уведомления и проверить код+тесты, затем Electron/frontend.
- Gateway принят только как локально проверенный протокольный модуль; реальная интеграция/качество/≤1s авария/30min/перевод/импорт/удаление legacy остаются незавершёнными.

## Миграция Soniox — реализация начата
- Пользователь подтвердил `docs/SONIOX-MIGRATION-SPEC.md` и разрешил реализацию. Реальные API/микрофон только пользователь; commit/push не разрешены.
- Перед изменениями сохранён source snapshot `.runtime/soniox-migration/source-before.tar.gz` (234 source files), SHA256 manifest `source-before.json`. Git main, большой существующий dirty tree; пользовательские изменения не сбрасывать.
- Свежий baseline: recorder **8 passed**, TypeScript/build exit0. Full frontend **363 passed/1 failed** (LocalModelsBrowser polling line293). Backend collection **567 collected/1 error**: test_activity_log_api imports missing gateways.asr.OPERATION; `.runtime/soniox-migration/backend-collection.log`. Это исходные результаты, не новая миграционная приёмка.
- Parent ASTRA изменил `backend/src/audiohelper/ingestion.py::_store_audio`: fsync файла перед rename и каталогов после; ошибки не ACK, retry возможен. `backend/tests/test_audio_playback_api.py::test_store_does_not_acknowledge_failed_disk_sync_and_allows_retry`: RED2 (ожидалось500, было201), затем GREEN. `backend/.venv/bin/python -m pytest backend/tests/test_audio_playback_api.py backend/tests/test_audio_ingestion.py -q` →49passed; scoped Ruff passed. Ранее опечатка test_ingestion.py дала no tests; исправлена. Полная power-loss гарантия НЕ достигнута: SQLite NORMAL, 5s capture windows и Darwin full sync требуют следующего среза.
- Gateway исполнитель запущен через Codex CLI, actual header `gpt-5.6-sol`, medium, provider openai, workspace-write; session `01a0a0da-07f1-7c13-b9a6-2122921eba7b`, process `proc_f5ff6bcdff15`. Brief `.runtime/soniox-migration/gateway-brief.md`, ожидаемый отчёт `gateway-report.md`/`gateway-last.txt`. Только gateway/tests/smoke/зависимость; не принят parent, платные прогоны запрещены. Следующий шаг — проверить его результат, локальные тесты и review, затем основной live/storage/migration срез.
- Официальные Soniox WS/async docs проверены: stt-rt-v5, stt-async-v5; пустой frame + finished:true, финальные токены delta/partial заменяется, processed watermarks,300min. Детали темпа/частот ещё проверяются исполнителем. Graph generation2026-09-08 устарел: get_code_snippet replace_chunk_segments вернул неверный участок из новых исходников; source fallback обязателен. Реальное определение repository.py:1330, не182.

## Предыстория: спецификация миграции Soniox
- Итог последовательного интервью сохранён в `docs/SONIOX-MIGRATION-SPEC.md`; документ представлен на итоговое подтверждение. Код не изменялся, модели не удалялись, реализация не начата.
- Soniox — единственный ASR для live/import/gap recovery; старые/experimental пути и скачанные для SKAZ веса подлежат удалению после проверки принадлежности, пользовательские данные сохраняются. OpenRouter — текстовые задачи и отдельный асинхронный перевод с сохранением английских терминов.
- Реальные API-прогоны выполняет пользователь; агент готовит реализацию, автотесты и сценарии. В этом этапе тесты/API/микрофон не запускались; запись markdown подтверждена инструментом, прежние результаты ниже не являются новой проверкой.
- Следующий шаг: итоговое подтверждение спецификации, затем проверка актуального git/исходников и официальных Soniox-контрактов. Исходная записка по main частично устарела относительно независимого audio storage и плеера. Не стирать существующие незавершённые изменения.

# Предыдущий handoff — frontend redesign

## Статус на момент записи
`npm run typecheck` — exit 0. `npm run test` — **366/366 passed** (36 files). `npm run build` — успешен.
Ничего не закоммичено, push не делался.

## Что сделано в этом заходе (Model assignment redesign)
Три карточки (Transcription/Assistant/Notes) были одинаковыми <select>-парами. Переделаны в
браузерно-вкладочный UI:
- **Provider tabs** (`.provider-tabs`) — один таб на провайдера, доступного для этой задачи.
  Иконка + `PROVIDER_LABELS` вместо голого id (`'openai'` → `"OpenAI"`).
- **`ProviderIcon.tsx`** — реальные логотипы через `simple-icons` (npm-пакет, установлен по
  прямому запросу пользователя) для `anthropic`, `openrouter`, `claude-code` — единственные три
  провайдера из семи, у которых в simple-icons есть настоящий бренд-значок. `openai` **не имеет**
  иконки в simple-icons (сознательно убрана из библиотеки), а `openai-compatible` и оба локальных
  провайдера (`local-whisper`, `local-gigachat-mlx`) — не бренды вовсе. Для всех них — честный
  круглый бейдж с первой буквой (`.provider-icon--fallback`), не выдуманный логотип.
- **Model list** (`.model-list`, role="listbox"/"option") заменил `<select>` — карточка со
  списком моделей текущего провайдера, бейдж Verified/Unverified справа. Custom-ID режим не
  тронут (тот же текстовый input).
- **Поиск скоуплен на активную вкладку** — прямое решение пользователя в грилле (глобальный
  поиск по всем 7 провайдерам потребовал бы грузить все каталоги разом при открытии страницы;
  отклонено как дорогое).

### Изменённые файлы
- `frontend/src/components/settings/ProviderIcon.tsx` (новый) — `ProviderIcon`, `PROVIDER_LABELS`.
- `frontend/src/components/settings/ProfileEditor.tsx` — provider select → tabs, model select →
  listbox rows.
- `frontend/src/components/settings/ProfileEditor.test.tsx` — `selectProviderTab`/
  `selectModelOption` хелперы заменили `user.selectOptions`, обновлены имена ролей/лейблов.
- `frontend/src/components/settings/SettingsPanel.test.tsx` — тест на 3 async catalog эффекта
  теперь проверяет `getAllByRole('listbox', {name: 'Model'})` вместо старого select-плейсхолдера.
- `frontend/src/styles/app.css` — `.provider-tabs(__tab/--active)`, `.provider-icon--fallback`,
  `.model-list(__row/__name/__badge)`.
- `scripts/visual-capture.smoke.spec.ts` — добавлен клик в Model assignment + скриншот
  `07-model-assignment.png`.
- `package.json`/`package-lock.json` — добавлена зависимость `simple-icons` (npm install по
  прямому запросу пользователя).

`npm run test` — **366/366**, typecheck и build чистые, скриншот подтверждён вручную (vision).

## Что сделано в предыдущем заходе (Settings redesign)
Полная переделка `frontend/src/components/settings/SettingsPanel.tsx` под референс пользователя
(вертикальный список разделов слева, контент справа во всю ширину, вместо одной длинной ленты):

- **System** — Microphone (переехал из RecorderBar), Appearance, Interface language (заглушка),
  Transcript/Answer language, Experimental contextual local mode + режим для новой записи.
- **Model assignment** — три карточки `ProfileEditor` (asr/agent/notes), без поля API key
  (оно теперь только в API keys). Добавлено честное предупреждение `missingKey` в `ProfileEditor.tsx`:
  если провайдер облачный (`needsKey()`) и ни в драфте, ни в сохранённом профиле
  (`profile.has_api_key`) ключа нет — карточка показывает "No API key set for {provider}."
  Ключ пустой строкой в драфте (только что стёрт) тоже считается отсутствующим.
- **API keys** — список используемых облачных провайдеров (`ProviderCredentials`), локальные
  провайдеры не показываются вообще (`local-whisper`/`local-gigachat-mlx` не требуют ключа).
  Если все назначенные провайдеры локальные — "No cloud providers are assigned yet."
- **Local models** — `LocalModelPreparation`, управляется выбором ASR-модели из Model assignment.

**Явного чекбокса cloud_consent больше нет** (прямое решение пользователя: "опенсорсный проект,
ответственность не беру, убрать совсем"). `cloud_consent` пересчитывается на каждый Save как
`usedProviders.some(p => needsKey(p))` — честно отражает факт "хоть один назначенный провайдер
облачный", не зависит от истории ввода.

**Индикатор пригодности модели под железо — НЕ СДЕЛАН, сознательно отложен.** Причина: реальные
данные (`hardware()` → `physical_memory_bytes`/`required_memory_bytes`) в бэке есть только для
`local-gigachat-mlx`; для `local-whisper` (более используемый локальный провайдер) в API/каталоге
нет ни требуемой памяти, ни размера модели — оценку неоткуда взять честно. Пользователь подтвердил
это решение явно в грилле. Следующий шаг для этого пункта — бэк-задача: добавить
`required_memory_bytes` для whisper-чекпоинтов, потом фронт может отрисовать 🔴🟠🟢.

## Изменённые файлы
- `frontend/src/components/settings/SettingsPanel.tsx` — переписан целиком (nav-rail + content layout).
- `frontend/src/components/settings/SettingsPanel.test.tsx` — тесты переписаны под секции-табы
  (`goToSection` helper), добавлены тесты на API keys раздел, микрофон, честный пересчёт
  `cloud_consent` при сохранении.
- `frontend/src/components/settings/ProfileEditor.tsx` — добавлено поле `missingKey` + рендер
  предупреждения.
- `frontend/src/components/settings/ProfileEditor.test.tsx` — добавлен `describe('ProfileEditor
  missing API key warning')`, 3 теста.
- `frontend/src/styles/app.css` — новые классы `.settings-layout`, `.settings-nav`,
  `.settings-nav__item(--active)`, `.settings-content`, `.settings-logo` (прижат к низу колонки
  через `margin-top: auto`), `.drawer__panel--settings` (шире стандартного drawer).

## НЕ ЗАВЕРШЕНО / ждёт ответа пользователя
**Баг Record/+** (из последнего сообщения пользователя, ДО текущего захода про Settings):
"кнопка рекорд продолжает запись а не создает новую запись. Новую запись создает + в боковом но
не запускается запускается только через record". Корень найден: `store.ts:839-844` — если
выбранная сессия `stopped` и в ней есть аудио (`selectedHasAudio`), Record молча создаёт новую
сессию вместо dozаписи в текущую. Задан уточняющий вопрос (переспрошен дважды, ответа от
пользователя пока не было в рамках этого захода):
- Вариант А (простое, чистый фронт): `+` сам сразу запускает запись новой сессии — убираем лишний
  клик, Record продолжает вести себя как сейчас (стартует новую при stopped+has audio).
- Вариант Б (append в существующую сессию): требует правки бэка — sequence чанков, отмена
  `source_ended` для contextual_local, продолжение таймера длительности. Не входит в "только дизайн".

**Действие при следующем заходе**: если пользователь ответил на вопрос — реализовать выбранный
вариант. Если нет — переспросить конкретно (А или Б), не гадать.

## Известные пределы (сознательно, не баги)
- Индикатор пригодности железа для локальных моделей — только для gigachat-mlx (см. выше).
- Языковой разделитель в ленте не появится, если марка создана до первого финализированного сегмента.
- Прогресс-бар расшифровки грубый (`max(segment.end_ms)/elapsedMs`), не учитывает паузы/gaps.
- i18n интерфейса отдельным этапом, сейчас всё на английском (тесты завязаны на англ. строки).

## Как проверить
```
npm run typecheck && npm run test && npm run build
npx playwright test scripts/visual-capture.smoke.spec.ts   # скриншоты в .runtime/shots/
```
